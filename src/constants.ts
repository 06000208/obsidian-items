/**
 * Simple boolean production environment check
 */
export const dev = process.env.BUILD !== "production";

/**
 * Size used for svg icons inside obsidian's nav-action-button elements
 */
export const buttonSize = 20;

/**
 * Size used for svg icons inside obsidian's tree-item-icon elements
 */
export const treeSize = 8;
